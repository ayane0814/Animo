<div class="container-fluid">
    <h2>投稿一覧</h2>
    <div class="row">
        <% @posts.each do |post| %>
            <%= render "public/posts/index", post: post %>
        <% end %>
    </div>
</div>
<div class="d-flex justify-content-center">
    <%= paginate @posts %>
</div>