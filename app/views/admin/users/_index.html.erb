<table>
    <tr>
        <td>会員ID</td>
        <td>名前</td>
    </tr>
    <% users.each do |user| %>
        <tr>
            <td><%= user.id %></td>
            <td><%= link_to user.name, admin_user_path(user) %></td>
        </tr>
    <% end %>
</table>